{{ define "Show" }}
  {{ template "Header" }}
    {{ template "Menu"  }}
    <h2> Register {{ .Id }} </h2>
      <p>First Name: {{ .FirstName }}</p>
      <p>Last Name: {{ .LastName }}</p>
      <p>Age: {{ .Age }}</p>
      <p>Date Joines: {{ .DateJoined }}</p>
      <br />
      <a href="/editPerson?id={{ .Id }}">Edit</a></p>
  {{ template "Footer" }}
{{ end }}